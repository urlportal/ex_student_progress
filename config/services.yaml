# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.cache.key.list_courses: 'courses_list'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/**/Domain/Entity/'
            - '../src/**/Domain/ValueObject/'
            - '../src/Application/DTO/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\Infrastructure\Persistence\Repository\:
        resource: '../src/Infrastructure/Persistence/Repository/'

    Symfony\Bridge\Monolog\Handler\ElasticsearchLogstashHandler:
        arguments:
            $endpoint: '%env(ELASTICSEARCH_URL)%'
            $index: 'app-logs'
            $elasticsearchVersion: '9.0.0'
            $bubble: true

    monolog.formatter.json:
        class: Monolog\Formatter\JsonFormatter

    App\Infrastructure\Logger\SensitiveDataMaskProcessor:
        tags:
            - { name: monolog.processor, priority: 200 }

    App\Infrastructure\Logger\RequestIdProcessor:
        tags:
            - { name: monolog.processor, priority: 100 }

    App\Infrastructure\EventListener\RequestIdListener:
        tags:
            - { name: kernel.event_listener, event: kernel.request, method: onKernelRequest, priority: 100 }
            - { name: kernel.event_listener, event: kernel.response, method: onKernelResponse, priority: -100 }
